
/*
* #include <SFML/Graphics.hpp>
using namespace sf;
void main()
{
    //Open window
    RenderWindow window(VideoMode(1920, 1080), "Pac man", Style::Default);
    Event event;
    Texture Background;
    Background.loadFromFile("images/pacman.png");
    Sprite BackgroundImage(Background);
    BackgroundImage.setPosition(0, -48);
    /**ojo.loadFromFile("images/jojospritesheet.png");
    Sprite Jojo(jojo);*/
/*IntRect uvRect;
Vector2u imageCount;
Vector2u currentImage;
float switchTime, totalTime;

//Game loop
while (window.isOpen())
{
    while (window.pollEvent(event))
    {
        switch (event.type)
        {
        case Event::Closed:
        {
            window.close();
            break;
        }
        case Event::KeyPressed:
        {
            if (event.key.code == Keyboard::Escape)
            {
                window.close();
            }
            break;
        }





        }
        while (true)
        {
            if (event.key.code == Keyboard::D)
            {
                BackgroundImage.setPosition(5, 0);
            }
            else if (event.key.code == Keyboard::A)
            {

                BackgroundImage.setPosition(-5, 0);
            }


        }




        window.clear();
        window.draw(BackgroundImage);

        window.display();
    }
}
}
*/

